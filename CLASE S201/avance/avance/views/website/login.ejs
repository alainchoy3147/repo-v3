<% layout("layouts/access") %>
    
    <br>
    <div class="alerta-roja d-none" role="alert" id="alertMessage">
        Contrasena Incorrecta
    </div>
    <div class="alerta-warning d-none" role="alert" id="warningMessage">
         Llena los Datos
    </div>

    <div class="alerta-pass d-none" role="alert" id="loginMessage">
        ¡Inicio de sesión exitoso!
    </div>
    
    <!-- INPUTS USUARIO Y PASSWORD -->
    

    <label for="txtUser">Usuario</label>
    <br>
    <input class="input" type="text" name="user" id="txtUser" placeholder="Ingrese su Usuario">
    <br>
    <label for="txtPassword">Password</label>
    <br>
    <input class="input" type="password" name="user" id="txtPassword" placeholder="Ingrese su Usuario">
    <br><br>
    <button class="btn" onclick="ingresar()">
        WOJOJOW
    </button> <br>

    <!-- BOTON Y VINCULO -->
    <div class="center">
      <a href="/reset-password">Has Olvidado tu Contrasena?</a>
    </div>
    
    <br>
    <!-- BLOQUE DE BIENVENIDA CON TEXTO -->
    <h3>BIENVENIDO A R3L4X</h3>
    <p class="fondo-gris">
      Estimados usuarios de la comunidad ULima: <br>

      Para resolver sus consultas sobre las plataformas Blackboard y Zoom, ponemos a su disposición los siguientes canales de atención: <br>
      <br>
      Corro electrónico: soporteaulas@ulima.edu.pe <br>
      Teléfono: 01 437 8800 <br>
      Anexo (en campus): 500 <br>
      Presencial: Sótano Edificio L2. <br>
      Horario: Lunes a viernes 7-22hrs, Sábados 7-13hrs
    </p>


    <script>
        function ingresar(){
            let username = document.getElementById("txtUser").value;
            let password = document.getElementById("txtPassword").value;
            
            if(password == "" || username == ""){
                let warningtMessage = document.getElementById("warningMessage")
                warningMessage.classList.remove("d-none")
                console.log(username)
                console.log(password)
                warningMessage.innerHTML = "PUTO"
            }else if(password == "123" || username == "admin"){
                let loginMessage = document.getElementById("loginMessage")
                loginMessage.classList.remove("d-none")
                console.log(username)
                console.log(password)
                loginMessage.innerHTML = "GGWP"

                document.getElementById("txtPassword").disabled = true
                document.getElementById("txtUser").disabled = true

                setInterval(() => {
                    window.location.href = "/home";
                },  5000);
                
            }else{
                let alertMessage = document.getElementById("alertMessage")
                alertMessage.classList.remove("d-none")
                console.log(username)
                console.log(password)
                alertMessage.innerHTML = "PUTO"
            }

        }

        document.addEventListener("DOMContentLoaded", function () {

        let fondos = [

            "/assets/img/background1.jpg",
            "/assets/img/background2.jpg",
            "/assets/img/fondo-bb.png"
        ];

        setInterval(() => {

        let indice = Math.floor(Math.random() * 3);

        document.body.style.backgroundImage = `url('${fondos[indice]}')`;

        },  10000);

    });
    </script>
